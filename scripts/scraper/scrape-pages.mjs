import scrape from 'website-scraper'; import PuppeteerPlugin from 'website-scraper-puppeteer'; const urls = ['about-us', 'menu', 'reservation', 'contact-us', 'our-services'].map(p => 'https://dilicious-demo.pbminfotech.com/pizza/' + p + '/'); scrape({ urls: urls, directory: './temp-scrape', plugins: [new PuppeteerPlugin({ launchOptions: { headless: true }, scrollToBottom: { timeout: 10000, viewportN: 10 }, blockNavigation: true })] }).then(() => console.log('Scraped!')).catch(console.error);

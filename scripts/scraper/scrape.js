const scrape = require('website-scraper'); const path = require('path'); const options = { urls: ['https://dilicious-demo.pbminfotech.com/pizza/'], directory: path.resolve(__dirname, '../../public/demos/dilicious-pizza'), request: { headers: { 'User-Agent': 'Mozilla/5.0' } } }; console.log('Starting full 1-to-1 clone...'); scrape(options).then(() => console.log('Scraping completed perfectly!')).catch((err) => console.error('Scraping failed:', err));

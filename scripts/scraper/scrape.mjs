import scrape from 'website-scraper'; import PuppeteerPlugin from 'website-scraper-puppeteer'; import path from 'path'; import { fileURLToPath } from 'url'; const __dirname = path.dirname(fileURLToPath(import.meta.url)); const options = { urls: ['https://dilicious-demo.pbminfotech.com/pizza/'], directory: path.resolve(__dirname, '../../public/demos/dilicious-pizza'), plugins: [ new PuppeteerPlugin({ launchOptions: { headless: 'new' }, scrollToBottom: { timeout: 10000, viewportN: 10 }, blockNavigation: true }) ] }; console.log('Starting Puppeteer clone...'); scrape(options).then(() => console.log('Scraping completed!')).catch((err) => console.error('Scraping failed:', err));
